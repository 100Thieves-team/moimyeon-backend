# 결정

- 생성 YAML을 수동 수정하지 않고 원본 REST Docs 파라미터 descriptor를 공통 함수로 통일한다.
- 정상·기본값·E400 사례가 동일한 선택 여부와 정상 계약 설명을 사용하도록 한다.
- 정상 조회 테스트에서 두 파라미터를 모두 생략해 기본 20건 조회가 문서 생성과 함께 검증되도록 한다.
- 문서의 문자열 쿼리 타입과 서버의 양수 커서·페이지 크기 기본값 정책은 유지한다.
- 프론트 SDK 수정은 dev 문서 게시 후 진행한다. 현재 PR에서 운영 호출 성공을 주장하지 않는다.
